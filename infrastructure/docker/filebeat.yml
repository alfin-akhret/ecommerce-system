filebeat.inputs:
  - type: container
    paths:
      - /var/lib/docker/containers/*/*.log

setup.kibana:
  host: "kibana:5601"

processors:
  - add_docker_metadata: {}

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]
